# Monorepo Restructure — Handoff Notes

Generated by the monorepo-restructure subagent.

---

## Files Moved

| Source (original app/) | Destination (repo/) |
|------------------------|---------------------|
| `app/` (entire directory) | `apps/web/` (via rsync, excluding `node_modules/`, `dist/`, `.git/`, `package-lock.json`) |
| `apps/web/client/src/data/workflows.json` | `packages/shared/src/data/workflows.json` (copied; original left in place for Vite alias fallback) |
| `apps/web/shared/schema.ts` | Content extracted into `packages/shared/src/schemas.ts` (original kept at `apps/web/shared/schema.ts` for `@shared/*` alias backwards compat) |

---

## Imports Rewritten

### `apps/web/server/routes.ts`
- `from "@shared/schema"` → `from "@gha-dispatcher/shared"`
- `import workflowsData from "../client/src/data/workflows.json"` → `import { workflowsCatalog as workflowsData } from "@gha-dispatcher/shared"`

### `apps/web/client/src/components/DispatchForm.tsx`
- `from "@shared/schema"` → `from "@gha-dispatcher/shared"`

### `apps/web/client/src/components/WorkflowTabs.tsx`
- `from "@shared/schema"` → `from "@gha-dispatcher/shared"`

### `apps/web/client/src/components/RecentRuns.tsx`
- `from "@shared/schema"` → `from "@gha-dispatcher/shared"`

### `apps/web/client/src/lib/api.ts`
- `from "@shared/schema"` → `from "@gha-dispatcher/shared"`

---

## Config Changes

### `apps/web/vite.config.ts`
- Added `"@gha-dispatcher/shared"` alias pointing to `../../packages/shared/src/index.ts`
- Kept `@shared` alias for backwards compat (anything not yet migrated resolves locally)

### `apps/web/tsconfig.json`
- Added `"@gha-dispatcher/shared"` and `"@gha-dispatcher/shared/*"` path mappings pointing to `../../packages/shared/src/`

### `apps/web/script/build.ts`
- Added `@gha-dispatcher/shared` to the esbuild `allowlist` (so it bundles TS source instead of externalising it)
- Added `alias: { "@gha-dispatcher/shared": <absolute path to packages/shared/src/index.ts> }` to the esbuild options

### `apps/web/package.json`
- `name` changed from `rest-express` → `@gha-dispatcher/web`
- `version` bumped from `1.0.0` → `0.1.0`
- Added `"@gha-dispatcher/shared": "workspace:*"` to `dependencies`

---

## New Files Created

| Path | Description |
|------|-------------|
| `package.json` | Root workspace config (`packageManager: pnpm@9`) |
| `pnpm-workspace.yaml` | Declares `apps/*` and `packages/*` workspaces |
| `turbo.json` | Turborepo pipeline: build, dev, lint, typecheck |
| `.gitignore` | Covers node_modules, dist, .turbo, .env, *.log |
| `README.md` | Quick-start, architecture, apps, packages, hosting, contributing |
| `HANDOFF.md` | This file |
| `.github/workflows/ci.yml` | Lint + typecheck on PR and push to main/master |
| `.github/workflows/deploy-preview.yml` | Placeholder — commented scaffold for PR previews |
| `packages/shared/src/schemas.ts` | Zod schemas and TypeScript types (WorkflowMeta, DispatchInput, DispatchPayload, RunSummary, DispatchLogRow, BranchSummary, WorkflowInventory) |
| `packages/shared/src/index.ts` | Re-exports all schemas, types, and `workflowsCatalog` |
| `packages/shared/src/data/workflows.json` | Moved from `apps/web/client/src/data/` |
| `packages/shared/package.json` | `@gha-dispatcher/shared` package manifest |
| `packages/shared/tsconfig.json` | TS config for the shared package |
| `packages/shared/README.md` | Package documentation with export table |
| `apps/docs/package.json` | Stub `@gha-dispatcher/docs` for Starlight (Astro + @astrojs/starlight devDeps) |

---

## Build Verification

```
pnpm install         ✅  809 packages, 0 errors
pnpm --filter @gha-dispatcher/web build  ✅

Client (Vite):
  dist/public/index.html              2.25 kB
  dist/public/assets/index-*.css     70.86 kB (gzip 11.54 kB)
  dist/public/assets/index-*.js     378.29 kB (gzip 122.13 kB)

Server (esbuild):
  dist/index.cjs  937.0 kB
```

---

## Next Steps

### Docs Subagent
1. Scaffold Astro + Starlight content inside `apps/docs/` — the `package.json` stub is in place with the correct devDependencies.
2. Add an `astro.config.mjs` with Starlight integration.
3. Create initial docs pages: Quick Start, Architecture, API Reference for `@gha-dispatcher/shared`.
4. Wire `pnpm dev:docs` to verify local preview.

### PR-Flow Subagent
1. Fill in `.github/workflows/deploy-preview.yml` — the commented scaffold documents required secrets and steps.
2. Required secrets: `PPLX_DEPLOY_TOKEN` (or `VERCEL_TOKEN`), `SUPABASE_URL`, `SUPABASE_ANON_KEY`.
3. Add a `post-preview-url` step using `actions/github-script` to comment the preview URL on the PR.
4. Consider adding a `pnpm check` / `tsc --noEmit` step to `ci.yml` once typecheck scripts are wired in `apps/web/package.json` (currently the web app exposes `check` not `typecheck`).

### Outstanding Known Issues
- `apps/web/package.json` does not yet define a `lint` or `typecheck` script — `pnpm -r lint` and `pnpm -r typecheck` will be no-ops for that workspace until added (suggested: `"typecheck": "tsc --noEmit"`, `"lint": "eslint client/src server shared"`).
- The original `apps/web/client/src/data/workflows.json` is still present (not deleted) as a fallback for the `@shared/*` Vite alias. Once the team confirms all imports are migrated, this duplicate can be removed and the `@shared` alias updated to point only at `packages/shared/src/`.
- `apps/docs/` has no Astro source files yet — `pnpm dev:docs` will fail until the docs subagent scaffolds the project.
